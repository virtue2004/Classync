# The tutor's app assumes the backend runs on the same machine by default
# (http://localhost:8000). Only set this if that's not true for your setup —
# e.g. the backend runs on a different port, or on a separate machine on
# the same network as this frontend.
VITE_DEFAULT_SERVER_URL=http://localhost:8000
